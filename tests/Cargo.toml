[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
token = { path = "../token" }
scanner = { path = "../scanner" }
ast = { path = "../ast" }
parser = { path = "../parser" }
format = { path = "../format" }
